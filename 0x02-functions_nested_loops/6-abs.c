#include "main.h"
/**
 *_abs - compute the absolute value of an int
 * @n: the int being check
 *
 * Return: the abs of the int
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
